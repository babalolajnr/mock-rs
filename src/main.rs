// use mock_rs::{Gender, Person, PersonTrait};

// use mock_rs::{Barcode, BarcodeTrait};

fn main() {
    // let first_name = Person::new().first_name(Some(Gender::Male));

    // println!("{}", first_name);
    // let ean = Barcode::ean(20);
    // println!("{}", ean);
}
